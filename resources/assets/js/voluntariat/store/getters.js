
export const activitats = state => {
    return state.activitats
}

export const activitat = state => {
    return state.activitat
}

export const token = state => {
    return state.token
}

export const user = state => {
    return state.user
}

export const roles = state => {
    return state.roles
}